<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="example_switch"
        android:summary="@string/pref_description_social_recommendations"
        android:title="@string/pref_title_social_recommendations" />

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_default_display_name"
        android:inputType="textCapWords"
        android:key="example_text"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_display_name" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="7"
        android:entries="@array/pref_general_audio_source"
        android:entryValues="@array/pref_general_audio_source_values"
        android:key="AUDIO_SOURCE"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_add_friends_to_messages" />

    <ListPreference
        android:defaultValue="3"
        android:entries="@array/pref_general_output_format"
        android:entryValues="@array/pref_general_output_format_values"
        android:key="OUTPUT_FORMAT"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_output_format" />

    <ListPreference
        android:defaultValue="1"
        android:entries="@array/pref_general_audio_encoder"
        android:entryValues="@array/pref_general_audio_encoder_values"
        android:key="AUDIO_ENCODER"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_audio_encoder" />

</PreferenceScreen>
